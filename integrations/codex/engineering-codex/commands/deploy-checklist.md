# 发布检查模板

适用于：发版前确认、灰度前确认、配置变更上线前确认。

把下面这段作为提示词交给 Codex：

```text
使用 engineering-codex-release-check 做发布前检查。

按以下清单输出：
1. [变更范围]
2. [配置变更]
3. [数据库影响]
4. [依赖影响]
5. [回滚路径]
6. [监控与告警]
7. [发布结论]

要求：
- 没证据的项一律标成“未确认”。
- 必须明确是否可回滚，以及回滚需要几步。
- 如果不满足上线条件，直接给“不建议发布”。
- 全部用中文。
- 不使用 emoji。
```
